// project import
import dashboard from './dashboard';
import data from './data';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, data]
};

export default menuItems;
