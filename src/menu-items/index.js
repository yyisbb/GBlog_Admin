// project import
import dashboard from './dashboard';
import support from './support';
import data from './data';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, data, support]
};

export default menuItems;
